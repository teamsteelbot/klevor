package spawner

import (
	"time"
)

const (
	// StartTimeout is the timeout to start the spawner
	StartTimeout = 5 * time.Second

	// StopTimeout is the timeout to stop the spawner
	StopTimeout = 5 * time.Second
)
